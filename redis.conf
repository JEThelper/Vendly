# Redis configuration for Vendly

# Increase max number of client connections to avoid connection limits
maxclients 200

# Enable lazy freeing of memory for evicted keys (non-blocking)
lazyfree-lazy-eviction yes
